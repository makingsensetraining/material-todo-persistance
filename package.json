{
  "name": "material-todo-persistance",
  "description": "A starter project for AngularJS and Material Design",
  "version": "0.0.1",
  "dependencies": {
    "body-parser": "*",
    "bower": "*",
    "express": "*",
    "mongoose": "*",
    "winser": "*",
    "uuid": "^2.0.1"
  },
  "engines": {
    "node": "0.10.x"
  },
  "scripts": {
    "postinstall": "bower install",
    "install-windows-service": "winser -i -a -s",
    "uninstall-windows-service": "winser -r -x -s"
  }
}
